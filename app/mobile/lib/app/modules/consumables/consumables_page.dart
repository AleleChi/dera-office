import 'package:flutter/material.dart';

class ConsumablesPage extends StatelessWidget {
  const ConsumablesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Consumables')),
      body: const Center(
        child: Text('Consumables page coming soon'),
      ),
    );
  }
}
