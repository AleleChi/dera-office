import 'package:flutter/material.dart';

class GasLogsPage extends StatelessWidget {
  const GasLogsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gas Logs')),
      body: const Center(
        child: Text('Gas logs page coming soon'),
      ),
    );
  }
}
